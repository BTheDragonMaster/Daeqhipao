import math
import pygame

BACKGROUND_BOARD = (255, 255, 255)
BACKGROUND_PANEL = (100, 100, 100)
HIGHLIGHT_BOARD = (170, 170, 170)

BLACK = (0, 0, 0)

SIZE = 1200, 800
WIDTH, HEIGHT = SIZE

PANEL_LOCATION = (HEIGHT, 0)
PANEL_SIZE = (WIDTH - HEIGHT, HEIGHT)

PANEL = pygame.Rect(PANEL_LOCATION, PANEL_SIZE)


SQUARE_WIDTH = int(HEIGHT / 13)

PIECE_PADDING = int(math.ceil(3 * SQUARE_WIDTH / 100))
SYMBOL_PADDING = int(math.ceil(28 * SQUARE_WIDTH / 100))

PIECE_SIZE = int(94 * SQUARE_WIDTH / 100)
SYMBOL_SIZE = int(44 * SQUARE_WIDTH / 100)

